import React from 'react';

const Options = ({ option }) => {
    return (
        <div className='d-flex'>
            <input type="radio" name='' />
            <label className='ms-3' htmlFor="">{option}</label>
        </div>
    );
};

export default Options;