import React from 'react';

const ShowChart = ({ data }) => {
    const { name, total } = data;   // data.name, data.total
    return (
        <div>
            
        </div>
    );
};

export default ShowChart;